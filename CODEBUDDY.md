# CODEBUDDY.md · ElcAgent 编码实施指南

> 本文件是项目根级规则，所有对话自动加载。
> 它的职责是给**地图、边界与实施顺序**，不是给完整规格——细节在 `docs/` 下，按需读取。
> **动手写代码之前，请先读完第 3 节。**

---

## 1. 项目是什么（30 秒版）

面向强监管能源行业的多智能体协同研判系统。气象灾害预警进入系统后，由多个智能体接力完成「数据口径统一 → 空间初筛 → 风险排序 → 规则校验 → 置信度评审 → 方案生成 → 简报初稿」，人工只做复核与签发。

**当前状态**：需求与设计阶段已完成（`docs/01` ~ `docs/10`），本文件指导进入编码。

**首要目标不是跑通业务，而是把三个核心机制做成真的**：

| 核心机制 | 文档 |
|---|---|
| 记忆系统（短期／长期／用户） | `docs/02`、`docs/10` |
| 多智能体通信（信封、路由、仲裁、熔断） | `docs/03` |
| 技能调用与管理（注册、校验、权限、限流、降级） | `docs/04` |

---

## 2. 第一原则：内容可以假，机制必须真

这是本项目一切取舍的依据。

- **必须真（机制与链路）**：记忆的写入与检索流水线（向量召回、去重、衰减）／消息总线与信封（路由、仲裁、熔断、trace）／技能的注册与调度（schema 校验、权限、限流、降级）。
- **可以假（内容与数值）**：记忆的具体内容（预置种子即可）／智能体的智能程度（规则或模板代替模型推理）／技能内部实现（返回固定值也能验证调度链路）／业务数据（几条硬编码样例即可）。

**判定标准**：把某处换成硬编码后，还能否说自己「实现了」这个模块？不能，那它就属于必须真。

---

## 3. 三条铁律（做假红线）

以下三条是**最容易在不知不觉中做成假的地方**。每条都给出"假的标志"与"真的标志"，编码时逐条自查。

### 铁律一 · 执行图必须由模板动态生成，不能写死

| | 标志 |
|---|---|
| ❌ 假的 | DAG 写死成顺序调用；每个意图硬编码一条固定链路；让模型自由生成智能体名字 |
| ✅ 真的 | 意图 → 能力需求模板 → 能力索引查表 → 依赖推导（`inputs ∩ outputs`）→ 拓扑排序 → 生成图；同一意图在不同深度下生成的图**不同** |

> 即使在 M1（最小闭环）阶段，也必须走「模板 → 图」这条路，哪怕图里只有一个节点。**不要为了快而先写顺序调用**。

### 铁律二 · 消息必须经信封与总线，不能直接函数调用

| | 标志 |
|---|---|
| ❌ 假的 | `send()` 内部直接调 `target.handle()` |
| ✅ 真的 | 信封序列化 → 投递到总线 → 按路由表分发 → 接收方反序列化；整条链路可异步、可并发、可失败重试、可落库 |

### 铁律三 · 技能必须注册与鉴权，不能是硬编码字典

| | 标志 |
|---|---|
| ❌ 假的 | 注册表写成 `SKILLS = {"a": func_a}`；任何调用方都能调任何技能 |
| ✅ 真的 | 注册表可扫描加载；输入输出受 schema 强校验；越权调用被拒并留审计；限流、缓存、降级层级逐项生效 |

---

## 4. 必读文档地图

**不要通读全部文档再动手。** 按当前任务查表读取。

| 当你需要… | 读这份 |
|---|---|
| 了解整体分层、智能体编队、Agent Card 规范 | `docs/01` |
| 实现记忆的存储、检索、冲突消解 | `docs/02`（分类与数据模型）+ `docs/10`（读写时序） |
| 实现消息信封、言语行为、通信模式、仲裁与熔断 | `docs/03` |
| 实现技能注册、清单、权限与治理 | `docs/04` |
| 定义表结构与接口契约 | `docs/05` |
| 了解前端页面与交互（MVP 之后） | `docs/06` |
| 查模型选型、技术栈、风险预案 | `docs/07` |
| 理解监控与交互如何统一、三级执行深度 | `docs/08` |
| 实现调度器与执行图生成 | `docs/09` |
| **开始每个里程碑前必读** | **`docs/11`**（分步实施计划，含每步验收标准） |

**优先级约定**：`docs/11` 的分步计划 > 本文件 > 其他文档。若三处冲突，以 `docs/11` 为准，并在汇报中指出冲突。

---

## 5. 技术栈与目录约定

### 5.1 技术栈（已锁定，不要替换）

| 层 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| Web 层 | FastAPI（MVP 阶段可暂不接入，先做进程内可运行的内核） |
| 数据校验 | Pydantic v2 |
| 编排运行时 | **自研轻量运行时**（异步），不引入 LangGraph / AutoGen 等框架 |
| 消息总线 | 进程内异步事件总线（接口预留可换 Redis Streams） |
| 关系库 | SQLite（SQLAlchemy 2.x，屏蔽方言差异） |
| 向量库 | 本地嵌入式向量库 或 pgvector，二选一 |
| 模型调用 | 统一走 ModelRouter 抽象，**禁止在业务代码里直连任何模型 API** |
| 测试 | pytest |

### 5.2 目录结构

```
ElecAgent/
├── CODEBUDDY.md              # 本文件
├── README.md
├── docs/                     # 设计文档 01~11
├── config/                   # 全部配置外部化
│   ├── model_router.yaml     # 模型档位与路由规则
│   ├── agents/               # Agent Card（一个智能体一张）
│   ├── skills_registry.yaml  # 技能注册表
│   ├── capability_templates.yaml  # 意图 → 能力需求模板
│   └── rule_packs/           # 规则包
├── src/
│   ├── contracts/            # 协议层（M0 冻结，后续不得随意改动）
│   ├── runtime/              # 多智能体运行时（bus / blackboard / supervisor / critic / registry）
│   ├── memory/               # 记忆系统（store / pipeline / conflict）
│   ├── agents/               # 各智能体实现
│   ├── skills/               # 技能实现（按分组）
│   └── model/                # ModelRouter 与各 provider 适配
├── tests/                    # 单元测试
├── scripts/                  # 自检脚本
└── Makefile                  # 统一入口命令
```

**约定**：`config/` 全部外部化，新增智能体或技能**只改配置不改内核**。

---

## 6. MVP 路线图

**分步推进，每步验收通过才进下一步。** 详细任务分解与验收标准见 `docs/11`。

| 里程碑 | 目标 | 关键验收 |
|---|---|---|
| **M0 契约与骨架** | 冻结协议层，能启动并加载注册表 | `make check` 报告已注册的智能体与技能数量；schema 非法时拒绝启动 |
| **M1 打通最小闭环** | 1 个主控 + 1 个执行智能体 + 3 个技能 + 1 条记忆，端到端跑通 | 投一条任务 → 动态生成图 → 执行 → 调技能 → 写记忆 → 出结果；trace 可重建 |
| **M2 动态执行图** | 8 个智能体 + 能力模板 + 依赖推导 + 契约校验器 | **同一意图在不同深度下生成的图可 diff 出差异**；幻觉能力被校验器拒绝 |
| **M3 三层记忆与记忆流** | 三层记忆 + 预取与蒸馏时序 + 快照语义 | `docs/10` 的 10 条验证清单全绿 |
| **M4 通信完整化** | 评审、熔断、仲裁、四级降级、循环检测、局部重规划 | `docs/03` 与 `docs/09` 的验证清单全绿 |
| **M5 技能治理** | 权限、限流、缓存、降级层级、健康度、启停开关 | `docs/04` 的验证清单全绿 |
| **M6 自检与可观测** | 合并全部自检、trace 重建与回放、事件流输出 | `make verify` 全绿 |

**MVP 之后**才做：前端（`docs/06`）、真实数据接入、模型档位真实化、监控路径的告警治理（`docs/08`）。

---

## 7. 每个里程碑的工作流

**统一按这六步执行，不要跳步：**

```
① 读 docs/11 中该里程碑的条目 + §4 指定的关联文档
② 提取该步的交付物清单与验收标准，向用户确认理解无误（一次确认，不要反复问）
③ 冻结或核对本步涉及的契约（contracts/ 下的模型与 schema）
④ 实现，自上而下：先契约 → 再内核 → 再具体实现
⑤ 跑该步验收 + 全量回归（上一里程碑的验收必须仍然通过）
⑥ 提交（commit message 见 §9），汇报结果，等用户确认后再进下一步
```

**关键纪律：不要跨里程碑提前实现。** 例如 M1 阶段不要写真实的数据生成，M3 阶段不要动前端。提前实现会让契约反复变动。

---

## 8. 禁止事项

| # | 禁止 | 原因 |
|---|---|---|
| 1 | 用现成多智能体框架（LangGraph / AutoGen / CrewAI 等）替代自研运行时 | 本项目要学的就是运行时本身；框架会掩盖机制 |
| 2 | 把执行图写成顺序调用或固定链路 | 违反铁律一 |
| 3 | 让模型直接输出智能体名字或自由规划执行者 | 会幻觉、不可验证 |
| 4 | 在业务代码里直连模型 API | 必须走 ModelRouter |
| 5 | **MVP 阶段写造数脚本或数据生成器** | 数据是"可以假"的部分；用一个线路、几个区段的硬编码样例即可 |
| 6 | 为"看起来完整"而实现未被要求的模块 | 只做当前里程碑范围内的 |
| 7 | 修改 `docs/` 下的设计文档 | 设计文档是输入，若要改请先提出并说明理由 |
| 8 | 跳过验收直接进入下一步 | 机制类的错误越晚发现越贵 |
| 9 | 引入未在 §5.1 列出的重量级依赖 | 未经确认的新依赖一律先问 |
| 10 | 把核心机制藏在"以后再重构"的注释里 | 就地做对，不留技术债 |

---

## 9. 工作方式与汇报格式

### 9.1 该怎么提问

**不要问设计文档已经回答的问题。** 先查文档，查不到再问。

值得提问的三类情况：
1. 文档之间相互冲突（指出冲突点与你的建议）
2. 设计未覆盖且影响架构的决策点（给出 2–3 个选项与权衡）
3. 验收标准无法客观判定（说明为什么无法判定）

**不值得提问的**：具体函数怎么命名、变量怎么组织、某个 map 用 `for` 还是推导式——这类自行决定。

### 9.2 汇报格式

每个里程碑完成后，按这个格式汇报：

```
## M{n} 完成情况

**交付物**：<文件清单，一行一个>

**验收结果**：
| 验收项 | 预期 | 实测 | 结论 |
|---|---|---|---|
（逐条列出，未通过的必须标红说明）

**回归**：上一里程碑的验收是否仍通过（是/否）

**与设计的偏差**：<如有，说明偏差原因；无则写"无">

**遗留问题**：<如有，标注影响范围；无则写"无">

**下一步建议**：<是否可进入 M{n+1}，有无前置项>
```

### 9.3 提交规范

- 每个里程碑至少一次提交，`feat(m1): ...` 格式
- **不得**在提交中包含 `.env`、`_local/`、任何凭据
- 提交前跑一次凭据与敏感词扫描

---

## 10. 完成定义（DoD）

一个里程碑只有在**同时满足**以下四条时才算完成：

1. 该步在 `docs/11` 中列出的**全部验收项通过**（不接受"部分通过"）
2. 上一里程碑的验收项**全部仍然通过**（无回归）
3. 有对应的**自动化测试或自检脚本**，能用一条命令复跑
4. **没有违反第 3 节的三条铁律**（需在汇报中显式声明自查结果）

---

## 11. 遇到设计未覆盖时的处理

设计文档不可能覆盖所有细节。按这个顺序处理：

| 情况 | 处理 |
|---|---|
| 属于实现细节（命名、结构、算法选择） | 自行决定，在汇报中一句话说明即可 |
| 属于契约变更（消息信封字段、Task 结构、技能 schema） | **先提出，说明为什么必须改**，等确认后再改 |
| 与三条铁律相关 | **一律先提出**，不得自行放宽 |
| 影响多个里程碑 | 先提出，不要边做边改 |

**核心原则：宁可停下来问一次，也不要自行发明架构。** 本项目要验证的正是架构本身，架构被悄悄改掉就失去了意义。
